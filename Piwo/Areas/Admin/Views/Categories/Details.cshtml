@model Piwo.Models.Categories

@{ 
    ViewData["Title"] = "Szczegóły";
}

<h2 class="text-info">Szczegóły Kategorii</h2>

<form asp-action="Details" method="post">
    <div class="p-4 rounded border">
        <div asp-validation-summary="ModelOnly" class="text-danger">

        </div>
        <div class="form-group row">
            <div class="col-2">
                <label asp-for="TypeOfProduct">

                </label>
            </div>
            <div class="col-5">
                <input asp-for="TypeOfProduct" class="form-control" />

            </div>
            <div class="form-group">
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Edytuj</a>
                <a asp-action="Index"class="btn btn-primary">Wróć do listy</a>
                @*<input type="submit" class="btn btn-primary" value="Wróć do listy" />*@
            </div>
        </div> 
    </div>
</form>
@section Scripts{
@{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}